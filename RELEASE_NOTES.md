# 版本更新记录

本文档详细记录 **练刻 LiftMark** 每个版本的更新内容，方便用户快速了解每个版本的新功能、修复和优化。

---

## v2.3.7 - 2026-07-07

### 核心更新

#### 记录与分析
- 训练分析页按“1RM 趋势 / 单个动作容量趋势 / 训练量趋势 / 训练频率趋势”排序，前两个图表可分别筛选动作。
- 趋势图横轴只显示有训练数据的时间点，未训练日期不再制造断点或假点；数值只在点击点位后显示。
- 日期筛选保留快捷按钮和“自定义”入口，自定义范围改为日历点选，不再手动输入日期。
- 补录页保持无系统标题和无系统返回按钮。
- 小组记录页的训练趋势整卡可点击进入分析，重复的小组分析和出勤入口已移除。
- 小组分析页把动作对比和出勤率入口移动到顶部时间筛选右侧，训练量 / 完成率 / 活跃度切换控件居中展示。
- 小组洞察现在从更丰富的数据驱动候选池中显示 4 条，减少固定文案重复。

### 验证
- 已通过：`npm run typecheck -- --pretty false`
- 已通过：`npm test -- --runInBand`，17 个套件 / 94 个用例
- 已通过：`npm run lint`，仅保留 `app/(tabs)/today.tsx` 的既有 warning
- 本次没有后端变更，不需要服务器部署。

---

## v2.3.6 — 2026-07-06

### 核心更新

#### 记录与分析
- 训练分析新增动作筛选，支持从真实训练记录中选择任意动作查看 1RM 趋势。
- 小组动作对比改为可搜索、可按肌群 / 器械筛选的动作选择，不再限制为前几个预设动作。
- 成员分析页右上角支持切换成员，图表、出勤和优势动作同步刷新。
- 折线图点位可点击查看具体数据；非连续训练日期按“空点跳过、有效点连接”的方式展示趋势。

#### 训练执行
- 删除休息结束后需要手动关闭的常驻提醒。
- 重量 / 次数输入在切换组时同步刷新，并继续使用成员设置的杠铃 / 哑铃加重单位。

#### 小组与头像
- 小组分析移除重复成员分析入口；训练量、完成率、活跃度切换会同步改变排行主指标和趋势图。
- 小组分析、出勤、动作对比和成员分析优先使用成员 profile 头像缓存。
- 账号头像同步补充当前训练成员 fallback，未绑定账号 ID 的“我”也能跟随账号头像更新。

### 验证
- 已通过：`npm run typecheck`
- 已通过：`npm run lint`（仅保留 `today.tsx` 既有 warning）
- 已通过：`npm test -- --runInBand src/tests/weight.test.ts src/tests/history.test.ts src/tests/chart-scale.test.ts`

---

## v2.3.5 — 2026-07-06

### 核心更新

#### 后台计划与动作库
- 后端新增系统内置计划和动作目录。
- `npm run db:seed` 会写入系统目录数据。
- 后台计划管理和动作库增加兜底返回，解决新库打开为空的问题。

#### 在线同练房间
- 新增小组在线房间 API，支持创建、加入、事件上报、离开和结束房间。
- 后台在线房间列表现在有服务端数据来源，移动端接入后即可看到房间。

#### 会员下发
- 修复后台发放来源和后端枚举不一致的问题。
- 支持用用户 ID、手机号、邮箱、练刻 ID 或唯一昵称发放会员。
- 会员页搜索支持练刻 ID 和邮箱。

#### 数据修正
- 数据修正从“只写记录”改为真实更新业务表。
- 服务端自动读取修正前的值，管理员不再需要手填旧值。
- 支持按 before/after 回滚。
- 小组成员关系新增 `role`、`user_id`、`left_at` 修正字段，用于处理历史串号成员关系。

#### 公告与头像
- 明确公告系统已接入，展示受发布状态、时间窗口、已读状态和 Today 页触发限制影响。
- `/uploads/avatars/` 目录访问返回说明，不再返回 route not found。

### 变更文件
- `apps/liftmark-api/src/db/systemCatalog.ts`
- `apps/liftmark-api/src/db/seed.ts`
- `apps/liftmark-api/src/modules/training-rooms/trainingRooms.routes.ts`
- `apps/liftmark-api/src/modules/catalog/catalog.routes.ts`
- `apps/liftmark-api/src/modules/admin/admin.extended.routes.ts`
- `backend/app/(admin)/membership/page.tsx`
- `backend/app/(admin)/corrections/page.tsx`
- `backend/后台管理问题排查与修复说明.md`

### 验证
- 后端 `npm run typecheck` 已通过。
- 后端 API `npm run build` 已通过。
- 后台管理前端 `npm run build` 已通过。

---

## v2.3.4 — 2026-07-06

### 🎯 核心修复

#### 默认本机成员
- 修复云同步后账号成员 `练刻3716` 被当成默认本机成员的问题。
- 首页、历史、体测、资料和训练总结统一优先使用本地训练身份，例如 `zhw`。
- 云端真实账号成员会绑定已有本地训练身份并保留本地昵称；无记录的重复账号成员会软删除。

### 📝 验证
- 已通过：`npm test -- --runInBand`（17 个套件 / 94 个用例）
- 已通过：`npm run typecheck -- --pretty false`
- 已通过：`npm run lint`

---

## v2.3.3 — 2026-07-06

### 🎯 核心修复

#### 账号数据恢复
- 同步拉取游标改为账号级，避免 188 的同步状态影响 176 从云端恢复。
- 云端记录应用失败时不推进同步游标，下一次同步会继续重试未落地的数据。
- 登录 / 切换账号的 fullPull 先同步小组和成员结构，再同步训练、计划、体测等业务数据。
- 当前账号云端记录可重新认领旧版本误归属到其他账号的本地记录，用于恢复历史串号数据。

#### 防止再次串号
- 登录后的归属修复收窄到身份结构表，不再批量修改训练、计划、体测数据归属。
- App 启动不再自动上传本地小组结构，避免把旧本地数据绑定到新账号。
- 后端 `/sync/push` 只允许更新当前用户已有的服务端记录，不信任跨账号 `serverId`。
- Push 成功后同步回写业务实体表的同步状态，减少后续 pull 冲突。

### 📦 变更文件
- 修改移动端同步链路：`pullService.ts`、`syncOrchestrator.ts`、`syncService.ts`、`authStore.ts`、`profileSyncService.ts`、`ownershipRepairService.ts`、`app/_layout.tsx`
- 修改后端同步接口：`apps/liftmark-api/src/modules/sync/sync.routes.ts`
- 新增同步恢复测试：`training-partner-app/src/tests/sync-pull.test.ts`
- 更新数据库和同步架构文档

### 📝 验证
- 前端：`npm test -- --runInBand`（16 个套件 / 92 个用例）、`npm run typecheck -- --pretty false`、`npm run lint` 通过；lint 仅保留 `LegalDraftPage.tsx` 既有 warning
- 后端：`npm run typecheck`、`npm run build` 通过
- 不需要新增数据库 migration

---

## v2.3.2 — 2026-07-04 `2dd7d3c`

### 🎯 核心修复

#### 本地数据库兼容升级
- 新增 `ensureLocalSchemaCompatibility()` 函数，自动检查并补齐 8 张核心表缺失的列
- 修复旧数据库升级后 `no such column: avatar_url` 崩溃问题
- 启动顺序修复：数据库迁移完成后才加载页面，避免查询时迁移未执行
- 新增迁移版本 13 (`local_schema_repair`)，调用全面 schema 修复

#### 头像上传全链路修复
- 修复 React Native 0.85 的 `Unsupported FormDataPart implementation` 错误
- 头像文件改用 XMLHttpRequest + Blob 方式上传，兼容新版 React Native
- 修复服务器端 uploads 目录权限问题

#### 同步诊断增强
- 新增本地数据库结构检查模块，显示每张表的头像字段是否存在
- 提供一键修复数据库结构按钮，无需卸载重装
- 显示已应用迁移版本号

### 📦 变更文件
- 新增 `schemaRepair.ts` — 可复用的 schema 兼容修复函数
- 新增 `syncDiagnosticsService.ts` — 同步诊断服务
- 修改 29 个文件，新增 1258 行，删除 265 行

---

## v2.2.1 — 2026-07-01 `869910b`

### 🎯 核心更新

#### 个人记录与历史详情
- 个人历史页删除重复"训练查询"模块，保留动作筛选、趋势和当天记录
- 动作趋势按 session 聚合，个人入口只展示当前成员 sets
- 训练详情右上角前置编辑入口，支持已完成记录新增动作、新增组、删组、基础信息编辑
- 历史补录从单动作/统一组数改为多动作、多组独立重量和次数录入
- 动作历史页删除平台 RPE 指标和 RPE 趋势图，保留容量、最佳重量和估算 1RM

#### 计划与今日训练
- 今日训练默认按当前计划已完成 session 自动跟随到下一个未完成训练日
- 计划首页删除重复区块，低频操作收进顶部更多菜单
- 计划详情右上角改为操作菜单，不再跳回计划页
- 创建页支持多训练日创建和通过 `editPlanId` 编辑用户计划
- `PlanRepository.updateUserPlan()` 新增替换式更新用户计划结构

#### 登录、成员与文案
- 移动端密码登录改为 `/auth/password/login`，支持手机号/练刻 ID
- 登录页增加密码显示切换，验证码入口文案改为注册/找回
- 成员新增失败文案优化
- 普通 App 页面移除技术化文案，改为用户语义文案

### 🐛 修复
- 修复登录密码登录 scope 错误
- 修复首次安装小米手机数据库初始化时序问题

### 📝 验证
- 前端：`npm run typecheck`、`npm run lint`、`npm test -- --runInBand` 通过
- 后端：`npm run typecheck`、`npm run build` 通过

---

## v2.2.0 — 2026-07-01 `910e64b`

### 🎯 核心更新

#### 新增体重数据模块 (Body Metrics)
- 新增 `body-metrics.tsx` 体重数据页面：快速记录、折叠围度、目标设置、变化摘要、训练关联和趋势图
- 新增 `BodyMetricsRepository` 体重数据仓储层（本地 SQLite + 云端接口）
- 新增 `body-metrics.types.ts` 体重数据类型定义
- 新增 `body-metrics-analysis.ts` 体重数据分析引擎
- 数据库 schema 新增 `body_metrics` 和 `body_metric_goals` 表
- SQLite migration v11 支持体重数据迁移

#### 训练执行组件增强
- 新增 `RestTimerPanel` 休息面板组件：倒计时、建议休息、实际休息秒数保存
- 新增 `RpeSelector` RPE 选择器组件：可折叠横向选择
- 新增 `SetNotesInput` 备注输入组件：训练中实时记录
- 新增 `WorkoutLiveStatsBar` 训练实时统计条
- `CurrentSetRecorder` 增强：重量/次数实时保存、RPE/备注折叠面板

#### UI 组件库扩展
- 新增 `Avatar` 通用头像组件：支持账号头像与训练成员 profile 统一
- 新增 `SecondaryPageHeader` 二级页面头部组件
- 新增 `chartScale` 图表缩放工具：MiniLineChart / MultiLineTrendChart 共享 Y 轴刻度

### 📊 数据与同步
- 数据库 schema 扩展支持体重记录和目标
- 同步队列增强：支持体重数据同步
- 历史分析引擎增强：支持体重趋势和训练关联分析

### 📝 文档
- 新增 `CONTRIBUTING.md` 贡献指南
- 新增 `RELEASE.md` 版本发布指南
- 新增 `RELEASE_NOTES.md` 版本更新记录
- 全量更新 120+ 模块文档和流程文档
- 更新 `.gitignore` 排除 `.zip` 构建产物

### 🐛 修复
- 修复 Login 页面密码登录 scope 错误
- 修复首次安装小米手机数据库初始化时序问题

---

## v1.2.0 — 2026-06-30

### 🎯 核心更新

#### 训练执行页全面重构
- 顶部导航栏改为浅色背景，显示训练标题和进度
- 整体从深色主题改为浅色主题，提升可读性
- 布局重排：统计条 → 动作 Hero → 成员轮换 → 当前记录 → 休息卡 → 已完成组 → 轮换顺序 → 进度条
- 新增"上一个动作"按钮，支持前后切换动作
- 成员轮换：点击头像可切换当前记录人
- 训练进度条：支持点击任意动作节点跳转

#### 记录卡片优化
- 重量/次数输入与 RPE/RIR 选择器左右分栏显示
- "完成本组"和"跳过休息"按钮并排，完成本组更醒目
- 已完成组支持内联编辑（直接修改重量/次数/RPE/RIR）和删除

#### 我的页面更新
- Banner 背景色从深色改为浅色
- Logo 切换为浅色背景适配版本
- 图标背景色对比度提升

### 🆕 新增组件
- `ExerciseHeroCard` — 动作 Hero 卡片
- `GroupMemberStrip` — 成员轮换条
- `CurrentSetRecorder` — 当前记录卡片
- `CompletedSetList` — 已完成组列表
- `RotationOrderCard` — 轮换顺序卡片
- `WorkoutProgressStrip` — 训练进度条（支持 card/dock 两种模式）
- `WorkoutStatsBar` — 训练统计条

### 🐛 修复
- 修复分析页面标题显示问题

### 📝 文档
- 更新训练执行页相关文档

---

## v1.1.0 — 2026-06-17 ~ 2026-06-22

### 🎯 核心更新

#### UI 全面重新设计
- 4Tab 布局重构（首页/计划/记录/我的）
- 主题系统全面优化（颜色、圆角、阴影）
- 应用 taste-skill / impeccable 设计规范

#### 新增功能
- 新增 MiniLineChart 迷你折线图组件
- 首页仪表盘重设计
- 训练记录和分析功能增强

### 🐛 修复
- 多处 UI 交互问题修复
- 性能优化和稳定性提升

---

## v1.0.0 — 2026-06-14 ~ 2026-06-15

### 🎯 核心功能

#### 项目初始化
- 完整训练伙伴应用源码
- 技术栈：React Native + Expo + SQLite

#### 功能模块
- **训练记录**：详细记录每次训练的组数、次数、重量
- **计划管理**：系统化的力量训练和增肌计划
- **历史分析**：查看训练历史和进步趋势
- **运动库**：100+ 个系统动作
- **成员管理**：支持多个团队成员
- **设置**：单位、记录方式、休息计时

### 🆕 版本历史

#### v1.0.5
- 优化历史分析和手动记录功能
- 更新品牌图标和启动画面资源
- 重构手动记录体验
- 新增训练进度分析模块

#### v1.0.4
- 全新历史分析界面
- 重构历史列表页
- 新增分析引擎

#### v1.0.3
- 优化训练计划创建流程
- 改进运动选择器
- 新增成员头像组件
- 扩展运动分类体系

#### v1.0.2
- 大幅扩展运动库和计划管理功能
- 新增运动选择器、模态弹窗组件
- 重构计划页、探索页、设置页

#### v1.0.1
- 新增 Android 原生构建配置
- 优化设置页和历史记录页
- 新增成员单位设置、成员管理页
- 新增经典 PPL 训练计划模板

#### v1.0.0-alpha
- 项目初始化，包含完整训练伙伴应用源码
- 技术栈：React Native + Expo + SQLite
- 功能模块：训练记录、计划管理、历史分析、运动库、成员管理、设置

---

## 版本对比

| 版本 | 主要更新 | 适用场景 |
|------|----------|----------|
| v2.2.0 | 体重数据模块、训练增强组件、UI组件库扩展 | 最新稳定版 |
| v1.2.0 | 训练执行页重构、UI 浅色主题 | 功能增强版 |
| v1.1.0 | UI 全面重新设计、新增折线图 | 基础增强版 |
| v1.0.0 | 核心功能完整、基础架构 | 基础稳定版 |

---

## 升级建议

### 从 v1.2.x 升级到 v2.2.0
- 新增体重数据模块，支持体重、围度记录和趋势分析
- 训练执行页新增休息面板、RPE 选择器、备注输入等增强组件
- UI 组件库扩展，头像和图表组件升级
- 建议备份数据后升级

### 从 v1.0.x 升级到 v1.2.0
- 体验重构后的训练执行页，操作更流畅
- 浅色主题更适合日常使用
- 新增多种交互功能，训练记录更便捷

---

## 问题反馈

如在使用过程中遇到问题，请通过以下方式反馈：
- GitHub Issues：https://github.com/a2531999713-spec/LiftMark/issues
- 邮箱：a2531999713@163.com

---

最后更新：2026-07-01

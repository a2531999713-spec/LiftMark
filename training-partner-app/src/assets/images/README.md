LiftMark image assets live here.

These files are local app assets selected from the project UI reference folder and renamed with LiftMark semantic names.
Screens should import them through `src/assets/images/index.ts` instead of hard-coding filenames.

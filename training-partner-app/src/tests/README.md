# Test Workspace

Sprint 1 only defines the test workspace and command wiring.

Add focused unit tests here as domain logic lands, starting with:

- `roundToIncrement`
- `estimateOneRM`
- `filterExercisesByRecovery`
- repository initialization and seed smoke tests

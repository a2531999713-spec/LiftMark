# v2.7.1 workout/report/history stability checks

- Complete a normal set and verify the workout stays active; verify only the final completion card ends it.
- Finish a workout with network unavailable and verify the local summary opens without waiting for sync.
- Open a persisted report and a legacy session without a report; both must render with correct calorie ranges or read-only fallback.
- Verify record center personal/group scope, cycle/free/manual filters, date filter, detail/report route, trend text and analysis entries.
- Enter manual training with one and two participants; the stored mode must be solo and group respectively, with no independent mode toggle.
- On an isolated test account fullPull cloud records and verify only exact same-account/same-group member mappings become visible. Do not write against account 176.

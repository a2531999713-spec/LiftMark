# 产品与业务设计文档

## 2026-07-01 记录筛选 / 训练执行 / 认证注册补充

- 个人记录页默认展示当前成员总训练量趋势，不默认选中某个动作；动作表现通过“全部动作”选择器进入。
- 小组记录默认展示小组总览，不自动进入动作详情；动作分析必须由用户选择真实练过的动作。
- 个人历史详情不得展示本机小组其他成员的 set；小组详情可以展示全部成员。
- 图表 Y 轴必须使用真实刻度和共享坐标，多折线图不得每条线单独缩放。
- 休息面板只保留一个主操作：倒计时中提前开始下一组，倒计时结束开始下一组，并保存实际休息秒数。
- 新动作没有建议重量时，用户可直接输入重量完成；下一组和后续训练可复用最近实际重量，避免重复提示“请先填写重量”。
- 登录页同时支持密码登录和短信验证码登录 / 注册；注册顺序与活动资格由服务端时间和服务端 sequence 判定。

## 2026-06-30 图表 / 身体数据 / 多小组 / 训练执行 Sprint

- 云端优先是当前产品方向：账号、小组、成员、计划、训练 session、训练组、动作替换、身体数据和关键设置默认具备同步路径；SQLite 用作缓存、副本和弱网训练保障。
- 训练执行页必须按真实训练现场设计：默认展示当前动作、当前成员、当前组 / 计划组、总完成组数；完成本组必须先保存当前 set，保存失败不能推进。
- 小组训练轮换按动作内 set number 优先：张三第 1 组 -> 李四第 1 组 -> 张三第 2 组 -> 李四第 2 组。
- 训练结束时，如果完成动作少于 2 个、完成组少于 3 组、时长少于 5 分钟、总量为 0 或大部分动作未完成，需要确认继续训练、保存记录或放弃本次。
- 我的训练记录是分析中心：顶部概览、趋势、动作筛选、日历当天记录、训练查询；不再把近期训练流水作为顶部核心模块。
- 成员资料保存按钮必须状态化：未修改禁用，修改且校验通过才可保存，保存中显示 loading；页面不展示解释区。
- 记录页和历史补录不默认展示休息秒；休息可作为训练现场内部数据和分析字段保留。
- 图表是数据产品组件，必须显示真实坐标、单位、空状态和安全绘图区；周趋势使用日期或周起止日期。
- 身体数据定位为训练辅助模块：快速记录体重，体脂/围度折叠，目标设置独立，摘要展示变化和训练关联，不做医学诊断。
- 头像产品逻辑：账号头像用于我的页，训练相关头像以成员 profile 为准；更新账号头像时同步当前小组第一位训练成员，保证训练、记录、小组分析一致。
- 小组记录默认展示小组总览，不默认进入任何动作；动作表现通过选择器查看真实练过的动作。
- 多小组以本地当前小组为上下文：今日训练、成员、记录、设置、头像同步和身体数据跟随当前小组，旧训练记录不迁移。
- 训练现场优先低摩擦：重量/次数/完成本组是主操作；RPE/备注/替换动作是可选低频能力。
- 完成本组后的休息状态必须展示倒计时、建议休息、已休息、下一组和下一位成员。
- 替换动作只影响本次训练，保存原计划动作和实际动作，不回写计划模板。

## 2026-06-30 主流计划推荐与训练页修复 Sprint

- 今日训练页的手动周次 / 训练日选择是本次开始训练的最高优先级输入；开始训练前会重新解析最新选择，避免使用旧周次缓存。
- 今日训练页的“动作筛选”会真实影响本次创建的动作快照：完整动作包含 A/B/C，精简辅助只保留 A/B，只做主项只保留 A，休息不创建 session。
- 计划页主界面不再直接铺开全部系统方案，只保留计划库弱入口，系统方案列表收进底部弹层。
- 新增“经典四分化增肌计划”：胸 + 三头、背 + 二头、肩、腿，每周 4 天，可复制为我的计划并设为当前计划。
- 历史训练详情默认只读，编辑记录和删除整次训练收进更多操作。
- 首页开始训练行动卡上移到今日摘要和状态选择附近，动作列表、搭子、小周概览等次要信息下移。
- 系统方案入口改为主流计划库：新手全身、Push Pull Legs、经典四分化、上肢 / 下肢、5x5、减脂保肌、恢复训练和居家哑铃；旧四练模板只保留历史兼容。
- 新用户登录后进入训练信息完善流程，填写资料后得到主推荐和备选计划；点击使用计划会复制系统方案并设置为当前计划。
- 头像展示统一到成员列表、训练首页、执行页、总结页、历史小组分析和账号相关页面。
- 小组历史主项数据可进入动作详情页，查看成员最好重量、容量、预估 1RM 和最近有效组。
- 退出登录收进账号设置页，并明确不会删除本机训练记录。

## 2026-06-26 首页与训练中 UI 重设计

### 首页重设计
- 首页从功能列表改为现代 Bento 风格布局，遵循 impeccable skill 设计原则
- Hero 卡片：深色背景 + 品牌色装饰，显示训练主题、计划名、动作数、成员数，可点击进入计划
- 快捷入口：3 个图标导航（训练记录、训练分析、切换计划），水平排列
- 训练内容：药丸标签展示动作列表，最多显示 6 个，超出显示"+N 更多"
- CTA 按钮：全宽品牌色，显示"开始训练"
- 进度条：显示训练完成百分比 + 周数 + 切换计划链接
- 设计原则：无边框卡片、品牌色强调、大量留白、清晰层次

### 训练分析页
- 顶部增加 "History / Analytics" 标题标签（品牌色小字）
- 下方显示 "训练分析" 大标题和当前成员信息
- 其余内容保持不变

### 训练中 UI 优化
- 已完成记录只显示当前成员的记录，不再显示所有成员
- 休息时间改为成员独立，每个成员有自己的倒计时
- 其他成员休息状态以小卡片形式显示

### 训练中组件重设计
- 移除"跳过动作"按钮（用户可通过底部进度条切换动作）
- 完成情况 改为折叠式设计，默认不展开，减少视觉噪音
- 休息倒计时集成到"跳过休息"按钮上，显示计时器徽章
- 删除独立的休息倒计时卡片，简化界面

### 已完成记录重设计
- 使用 chip 样式替代原有列表样式
- 重量和次数独立显示，不再被截断
- 完成状态使用清晰按钮态区分
- 编号徽章更醒目

### 动作列表重设计
- Dock 模式：进度条 + 动作 chip 标签
- Card 模式：垂直卡片列表，每个动作独立一行
- 当前动作高亮（品牌色边框 + "当前"徽章）
- 已完成动作显示绿色勾号

## 2026-06-24 手机号密码登录与验证码绑定重构

- 首次使用必须完成账号登录 / 注册，未登录不能进入首页、计划、记录、我的等主 Tab。
- 登录主路径为手机号 + 密码；不再把手机号验证码作为唯一登录方式。
- 注册必须使用手机号 + 验证码 + 密码 + 昵称，验证码用于验证并绑定手机号。
- 验证码后续也用于手机号绑定 / 换绑、找回密码等敏感流程，不作为“无密码自动注册”的主路径。
- 启动状态分层：`checking`、`unauthenticated`、`authenticated`、`offline_authenticated`。
- 有本地 session 时，即使服务器不可达，也允许进入默认主界面并显示“当前离线，已进入本机模式”。
- 离线进入时不主动拉取云端完整资料，不做全量同步，不清空 session，不清空 SQLite。
- 登录免费版可以真实训练、离线训练并保存本地 SQLite 记录；免费版限制集中在 `src/domain/auth/access-control.ts`，不散落到页面组件。
- Pro / 永久会员解锁高级历史、完整小组、高级分析、在线同练和完整云同步等权益入口；未接支付的购买动作必须显示开发中提示。
- 登录页是双态认证表单：密码登录展示手机号、密码和协议勾选；注册展示手机号、验证码、密码、昵称和协议勾选。
- 当前 UI 不展示继续浏览、预览模式、验证码一键登录、第三方登录、会员权益或小组协作卖点。
- 验证码发送失败必须转换为中文提示，不能向用户展示 `fetch failed`、`ConnectException`、服务器 IP 或 stack trace。
- 退出登录只清除账号 session，不清空本机 SQLite 训练数据；训练执行仍然云端优先 + 本地缓存，服务器失败不影响现场记录。

## 2026-06-24 我的页账号与训练身份改造

- 底部 Tab 保持四栏：首页、计划、记录、我的；`members` 和 `explore` 不作为主 Tab 展示。
- 我的页从旧设置中心升级为“账号 + 训练身份”信息架构：顶部主卡只展示用户、练刻 ID、登录状态、当前训练成员、所在小组和训练角色。
- 云同步、会员权益、激活码、账号安全都归入账号设置二级入口，不放在顶部主视觉中。
- 训练相关分组包括：我的训练身份、我的小组、训练偏好、训练数据；账号 user 与训练 member 的职责必须在训练身份页明确区分。
- 通用设置、计划导出、关于练刻作为独立分组；旧的试用模式、SQLite/seed 诊断、重建测试数据和重置默认计划不再面向普通用户展示。
- 当前 App 已接入真实后端密码登录、验证码注册、token 保存/刷新和 `/auth/me`；服务器不可用时保留本地训练能力。
- 云同步以 `src/sync/*` 作为边界；`local_sync_queue` 已接入训练 session / set 的待同步流程，训练现场继续先写本地缓存并异步推云。

## 2026-06-15 UI 一致性 / 计划入口 / 成员流程 Sprint

- 普通功能页禁止表单化：探索、计划、记录、设置和训练页弹层使用卡片、列表、标签、底部弹层或分组操作；复杂选择不使用系统 Alert。
- 探索页只展示少量推荐系统方案，PPL 入口可直接使用或提示已在我的计划中；未完成方案收进“更多方案开发中”摘要卡。
- 计划页系统方案展示为紧凑方案卡，“使用此方案”先确认复制到我的计划，复制成功后再询问是否设为当前计划。
- 训练页“切换计划”使用底部列表和二次确认，只列出我的计划，切换后刷新今日训练内容，历史记录不受影响。
- 记录页日期下训练记录默认使用摘要卡，主要动作只展示前 2 个和剩余数量，详情页查看完整动作。
- 新增成员保存后自动返回成员列表；成员上限集中配置，当前本地小组最多 5 人，达到上限时新增入口变为说明卡。
- 导出计划/数据后明确提示当前版本暂未保存到文件，并提供复制内容，不让用户误以为文件已保存。

## 2026-06-15 计划仪表盘 / 动作库 / 自定义动作 Sprint

- 系统动作库扩展为 100+ 个正确中文动作，覆盖胸、背、肩、腿、手臂、核心、热身/有氧等常用场景。
- 第一版动作库支持系统动作和本机自定义动作，计划创建和历史补录共用统一动作选择器；没有动作时可快速新建自定义动作。
- 计划页重做为当前计划仪表盘，展示本周执行、本周安排、我的计划摘要、计划工具和系统方案。
- 用户计划可删除，但系统方案、当前计划和最后一个用户计划不可删除；删除计划不删除历史训练记录。
- 建议重量支持按计划百分比或次数区间保守估算，缺少 1RM 时提示补充个人参数，孤立/器械动作提示参考历史重量。

## 2026-06-14 设置 / 历史 / 计划切换清理 Sprint

- 设置页定位为移动端设置中心，不直接编辑成员资料，不展示数据库调试式 JSON 预览，不暴露“周五策略”全局入口。
- 成员资料和加重单位都是入口与摘要：设置页展示全部成员，用户进入列表后再选择具体成员编辑。
- 计划导入支持 `.liftmark.json` 文件选择、格式校验、schema 校验、ID 重映射和 SQLite 落库；导入结果是“我的计划”，不覆盖已有计划，不导入训练记录、成员 1RM 或身体数据。
- 系统方案新增“经典三分化 PPL”，作为只读模板存在；用户必须点击“使用此方案”复制为“我的计划”后才能执行。
- 训练页当前计划卡提供“切换计划”入口，只列出“我的计划”，切换后刷新今日训练内容，历史记录继续使用训练时快照。
- 记录页训练查询改为紧凑训练摘要卡，展示日期/时长、动作数、完成组数、总训练量、成员口径、PR/估算 1RM/趋势标签，并点击进入历史详情。

## 2026-06-14 训练执行交互修正 + 记录/设置 UI 重设计

- 训练执行页只保留重量、次数、完成/跳过和备注，不再展示旧强度快捷输入。
- 记录页重新设计为更成熟的数据页：当前成员个人口径、个人概览卡、紧凑小统计、基础趋势、可点击日历和紧凑训练记录列表；小组汇总仍为开发中，不混用个人数据。
- 设置页重新设计为移动设置中心：顶部品牌/状态卡，下面按通用设置、训练设置、试用与激活、计划导出、计划管理、开发与诊断、调试保护分组。
- 搭子页主页面只保留一句本地小组说明，完整本地/云同步规则放入“了解本地小组”弹层。
- 图标资源只做背景和安全边距修复：桌面图标改为深色实底，adaptive 前景保持透明，splash logo 改为透明居中资源；不改变核心图形方向、品牌或 Android package。
- 记录页设计稿：`docs/ui/record-page-redesign.png`；设置页设计稿：`docs/ui/settings-page-redesign.png`。

## 2026-06-14 训练执行体验与记录口径修复

- 训练执行页完成本组后自动推进：有下一组则按计划休息时间进入倒计时，休息结束或跳过休息后进入下一组；当前动作完成后自动进入下一个动作；最后一个动作完成后进入总结。
- 训练执行页只保留重量、次数、完成/跳过和备注，不再展示旧强度快捷输入。
- 已完成组必须可返回编辑、删除或撤销上一组，编辑只更新原 set 记录，不新增重复记录。
- 补录训练时休息时间允许为空；空间歇不影响训练量、PR 和估算 1RM，也不参与后续训练密度或疲劳分析。
- 记录页默认统计当前成员个人数据，文案使用“当前成员”“我的本周训练量”“我的本周训练次数”“我的本周完成组数”。
- 小组汇总未实现前显示开发中说明，不能把个人数据和小组数据混在一起。
- 当前小组是本地小组：适合同一台设备多人轮换记录，数据保存在本机，不会自动同步到其他手机；组长可以查看本机保存的所有本地成员训练数据。未来云同步版本再支持账号登录、邀请成员、多设备同步和授权共享数据。
## 2026-06-24 云服务与账号接入补充

- “我的”页继续以账号、训练身份和小组为顶部主卡，不把云同步、会员权益或 Pro 状态做成顶部主视觉。
- 普通用户“我的”页不再显示训练数据导出、备份恢复、本地试用或开发测试激活码作为主功能。
- 密码登录、验证码注册、会员状态和激活码兑换已改为调用练刻后端 API。
- 未登录用户仍可继续本地训练；服务器不可用时，本地 SQLite 训练闭环不受影响。
- 云同步入口保留在账号设置下的云同步二级页，第一版只验证云服务连通性，完整本地同步队列后续接入。

## 2026-06-14 品牌迁移状态

- 产品品牌统一为“练刻 LiftMark”。
- 产品定位：你的力量训练计划执行器，记录每次训练，刻下持续进步。
- 旧名称不再作为当前产品品牌；“今日训练”可作为功能入口，不作为品牌名。
- 计划导入导出第一版使用 `.liftmark.json`，不导出成员 1RM、身体数据或训练记录。


更新时间：2026-06-12

## 1. 产品定位

“练刻 / LiftMark”是一个支持多人同练、计划自定义、自动重量计算、训练记录、周期进阶和计划分享的力量训练 App。

第一版不是普通单人训练记录工具，也不是 Excel 表格搬运，而是安卓本地可用的多人训练执行 App。

## 2. 用户角色

| 角色 | 需求 |
|---|---|
| 普通健身者 | 按计划训练、记录数据、获得下次建议 |
| 健身搭子 | 2-5 人一起练，同动作不同重量，轮流记录 |
| 进阶训练者 | 使用增力/增肌周期，关注 1RM、完成情况 和减量 |
| 私教/小团队 | 后续分配计划、查看完成情况 |
| 新手用户 | 套用模板，降低训练计划理解成本 |

## 3. 核心业务流程

- 首次使用：seed 系统方案和默认用户计划 -> 创建默认小组 -> 添加成员 -> 输入 1RM -> 进入今日训练。
- 今日训练：读取小组当前用户计划/周期/周数 -> 按星期生成动作 -> 按动作筛选过滤 A/B/C -> 按成员计算建议重量。
- 训练执行：创建 session -> 生成动作记录和 set -> 多人按动作轮换记录 -> 即时保存 SQLite。
- 动作替换：选择替代动作 -> 保存替代后的 exercise_id，同时保留 replaced_from_exercise_id。
- 训练总结：完成 session -> 统计完成度 -> 生成 progression_suggestions -> 进入总结页。
- 进阶建议：增力看完成率，增肌看次数区间、动作质量。

## 4. 功能列表

| 功能 | 状态 | 相关模块 | 说明 |
|---|---|---|---|
| 默认小组 | MVP | group | 第一版本机默认小组 |
| 成员管理 | MVP | member | 添加/编辑成员和 1RM |
| 系统方案库 | MVP | plan | 系统提供只读方案，用户点击“使用此方案”后复制为我的计划 |
| 我的计划 | MVP | plan | 用户从系统方案复制、空白创建或文件导入的可执行本地计划 |
| 今日训练 | MVP | plan, weight, recovery | 展示当前训练内容和建议重量 |
| 训练执行 | MVP | workout | 多人轮换记录 |
| 动作替换 | MVP | exercise, workout | 器械被占时替换 |
| 训练总结 | MVP | workout, progression | 完成度和下次建议 |
| 训练历史 | MVP | history | 近期训练、动作历史、建议 |
| 设置页 | MVP | settings | 品牌信息、小组状态、计划导出和调试信息 |
| UI 设计系统 | MVP | ui | 统一主题 token、基础组件、四栏底部导航和训练现场友好页面 |
| JSON 导出 | MVP | export | 本地数据导出、训练记录导出和 `.liftmark.json` 计划导出，预留 `.liftmark` |
| 云同步 | 延后 | sync | 第二/三阶段 |
| 计划编辑器 | 延后 | plan | 第一版只读为主 |
| 计划市场/教练模式 | 延后 | plan, group | 完整商业版 |

## 5. 页面结构

- 底部 Tab：首页、计划、记录、我的。
- 首页：今日训练任务中心，展示今天练什么、今日重点、当前搭子、本周简要概览和开始训练入口。
- 计划页：当前计划仪表盘、阶段进度、本周安排、我的计划管理、系统方案、创建计划、导入计划和导出计划。
- 记录页：训练历史、PR、预估 1RM、趋势、疲劳提示、近期训练和分析建议。
- 我的页：深色账号卡 + 训练档案、小组成员、偏好设置、账号设置四个主入口 + 关于练刻；退出登录放在账号设置页。HeroCard 小组和计划区域可点击跳转。关于练刻页面包含关于练刻、意见反馈、用户协议、隐私政策。
- 训练执行页：从首页进入，不显示底部 Tab，使用沉浸式深色全屏页面，突出当前动作、多成员记录卡、重量/次数 stepper、完成/跳过按钮、完成本组和自动保存状态。
- 总结页：完成度、本次训练总量、成员表现、预估 1RM、本次最佳动作、训练时长、下次建议和恢复建议。

## 6. 权限规则

第一版为本地单设备应用，不做账号登录和云端权限。数据模型保留角色：

- owner：组长，管理小组和计划。
- member：参与训练和记录。
- coach：后续教练模式预留。
- guest：后续只读查看预留。

## 7. 状态机

### 训练 session 状态

```text
draft -> in_progress -> completed
draft -> cancelled
in_progress -> cancelled
```

### 计划周期状态

```text
strength -> deload -> hypertrophy -> deload -> strength
```

### 进阶建议类型

```text
increase | maintain | decrease | deload | add_reps | maintain_or_decrease
```

### 恢复建议

```text
normal | remove_c | only_a | reduce_weight | deload | rest
```

## 8. 边界条件

- 用户跳过 1RM 时仍可进入 App，建议重量显示未设置。
- 周五默认休息，可选补弱；不练周五不需要补回来。
- 状态一般删除 C 动作，状态差只保留 A 动作，状态很差建议休息或减量。
- 器械被占时替换动作，但原计划动作不变。
- 历史记录必须保存当时的计划快照。
- 历史推算只能作为训练建议，不能写成医疗、伤病或绝对结论。
- `.liftmark.json` / `.liftmark` 计划文件不能包含训练记录、成员 1RM 或个人参数。
- 系统方案不是用户计划；用户必须点击“使用此方案”后才生成自己的训练计划。
- 训练记录不能直接绑定系统方案，只能绑定用户计划或训练时计划快照。

## 9. 异常流程

- seed 失败：提示重试，不进入半初始化状态。
- SQLite 写入失败：训练执行页显示错误并保留当前输入状态。
- 缺少成员：今日训练提示添加成员。
- 缺少计划：提示从系统方案复制、创建计划或导入计划。
- 无替代动作：显示空状态并允许返回。
- 导入计划版本不兼容：显示中文提示，不覆盖本地计划。
- 危险设置操作：必须二次确认。

## 10. 业务限制

- 计划是数据，不是代码。
- 计划模板和个人参数分离。
- 计划和训练记录分离。
- 多人逻辑从第一版就保留。
- 云端优先，本地 SQLite 作为缓存与离线副本。
- Domain 层不依赖 UI。
- 训练记录不能用 AsyncStorage 保存。
- Excel 训练计划只整理为后续 seed 数据设计说明，不硬编码进页面组件。

## 11. 未来规划

- 阶段 2：休息计时器、恢复评分、PR、日历、CSV 导出、一键复制上一组、临时加/删组、备份恢复。
- 阶段 3：账号系统、云同步、小组邀请、二维码加入、多设备同步、计划分享。
- 阶段 4：iOS。
- 阶段 5：计划编辑器、公开计划库、教练模式、数据分析、动作视频、订阅、计划市场和健康数据接入。

## 12. 需要人工确认的问题

- 第一版恢复评分是完整表单还是只做今日训练快速选择。
- 是否需要在 MVP 支持临时加组/删组，开发文档把它放在阶段 2。
- 动作质量下降字段是否在 MVP 的 set 记录中显式保存。
- 成员和训练记录删除是否采用软删除。

## 13. 可用性 + UI 落地 Sprint 补充

更新时间：2026-06-12

- 训练页周五默认休息不再锁死用户；用户可以手动选择 Day 1-4、补弱或自由训练。
- 周五策略字段继续保留用于兼容训练页，但设置页不再暴露该全局入口；后续应放到计划详情或训练页临时覆盖中。
- 记录页支持点按日期、月视图、查看某天训练、补录训练和进入历史详情。
- 历史详情支持编辑训练日期、标题、动作、组重量、次数、完成情况，并对删除组、删除动作和删除整次训练做二次确认。
- 计划页提供真实“创建计划”入口，第一版可保存一个基础训练日和动作设置为“我的计划”。
- 本地试用/激活状态已从普通用户主入口下线；激活码兑换迁移到“会员与激活”，以后端账号权益为准。
- 视觉方向同步 v2 参考图：浅底、本地训练图片 Hero、深色遮罩、珊瑚红主行动、大圆角卡片、两列输入和可点击日历。
- 图片素材已从项目 `docs/ui/` 参考素材中筛选并复制到 App 本地 assets，不依赖 Web 图片或第三方远程资源。

# Sync Opt-In Flow

更新时间：2026-06-24

```text
登录成功
  -> 检测本机成员 / 我的计划 / 训练记录
  -> 如有本机数据，提示“发现本机训练数据”
  -> 用户选择绑定到当前账号或暂时保留在本机
  -> 提示“开启云同步？”
  -> 开启云同步或暂不开启
```

当前限制：

- 本机数据绑定功能正在开发中，不会删除、覆盖或自动上传 SQLite 数据。
- 云同步入口可测试，完整自动同步队列后续接入。
- 训练现场仍然先写 SQLite，云端失败不能导致训练记录丢失。

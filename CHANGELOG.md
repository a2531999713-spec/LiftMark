# LiftMark (练刻) 更新日志

---

## v2.7.2 - 2026-07-14

**记录分析、首次引导、身体数据和计划加载一致性修复**

- 记录首页恢复数据驱动的训练趋势和小组洞察，不再只显示分析按钮。
- 登录后按账号引导状态及已恢复的成员/计划决定目的地，不再重复强制填写训练资料。
- 身体数据入口迁至“我的”；体重保存与成员档案最新体重保持一致，旧日期补录不覆盖更新数据。
- 首页与计划最近训练排除 `in_progress`、draft、cancelled 与无有效完成组的 session。

---

## v2.7.1 - 2026-07-14

**训练结束、报告、记录中心与云端个人记录稳定性修复**

- “完成本组”不再结束训练；结束训练仅由独立的最终确认入口触发，SQLite 完成后优先进入摘要，sync 留在后台。
- 训练报告改用 schema 一致的 `estimated_calories_min` / `estimated_calories_max`，旧训练没有报告时继续只读回退。
- 记录中心恢复趋势说明和个人/小组分析入口；手动补录按参与人数自动判定单人或多人。
- 稀疏数据柱图宽度限制为 8–24px；动作调整增加防重复提交和动作列表缓存。
- fullPull 的成员 ID 修复只接受同账号、同小组内 `local_member_id` / `remote_id` 精确映射，未新增数据库迁移、API 或部署。

---

## v2.6.0 - 2026-07-14

**训练提醒设置与本地通知闭环**

- 新增“我的 → 训练偏好 → 训练提醒”入口、状态摘要、训练日/时间/提醒方式、权限状态和测试通知。
- 按当前账号、小组、活动计划和计划周期生成本地 Expo 通知；保存前只取消本提醒保存的设备调度 ID，避免重复或误删其他通知。
- 通知点击安全进入今日训练页；周期完成/归档、登出和账号切换取消本机调度，恢复或重启后按当前账号重新建立。
- 新增 SQLite v24，仅保存不参与同步的 `notification_ids_json`；无 PostgreSQL migration、API 或服务器部署变更。

---

## v2.5.0 - 2026-07-11

**计划周期、训练报告详情与历史筛选**

- 新增训练报告详情页与训练结束/历史详情双入口；旧记录缺少报告时使用只读汇总，不静默补写。
- 新增计划周期卡、周期总结页、完成/归档确认和幂等 summary 重算。
- 记录页新增当前周期、具体历史周期、自由训练、补录筛选，改用账号和小组作用域内的单次聚合查询。
- 复用既有 SQLite v23 表和同步 registry；无 SQLite/PostgreSQL migration、API 或部署变更。
- 验证：TypeScript、ESLint、36 个 Jest 套件/177 个测试、Android arm64 Release 构建通过；188/176 真机账号验收待在安全测试环境完成。

---

## v2.4.0 - 2026-07-11

**核心架构收敛与安全加固**

- 三端目录明确为移动端、Fastify API 和 `management-console`，新增最小 `packages/shared` 契约包。
- 引入 AppScope、作用域 repository 入口、首页/训练 feature 边界、首页聚合查询和训练写入 flush 机制。
- 同步新增实体注册表、显式序列化、安装级设备 ID、协调器、冲突策略和服务端缺表健康检查。
- 注册必须验证短信验证码；生产 CORS 使用白名单，敏感认证/管理/上传接口增加限流。
- 本次不含 SQLite/PostgreSQL migration，不修改生产数据，不执行服务器部署。

---

## v2.3.7 - 2026-07-07

**移动端记录与训练分析交互修复**

### 修复
- 训练分析图表顺序调整为 1RM 趋势、单个动作容量趋势、训练量趋势、训练频率趋势；1RM 和单动作容量图表均支持动作筛选。
- 趋势图不再把未训练的日期 / 周显示在横轴上，点位数值默认隐藏，点击有效点位后才展示 tooltip 与高亮数值。
- 日期筛选恢复“自定义”按钮，并改为日历式范围选择，不再手动输入起止日期。
- 补录页继续隐藏系统标题和返回按钮，避免重复导航。
- 小组记录页整张“训练趋势”卡可点击进入分析页，并移除重复的小组分析 / 出勤率入口。
- 小组分析页顶部时间筛选右侧新增“动作对比”和“出勤率”入口；训练量 / 完成率 / 活跃度切换控件居中展示。
- 小组洞察改为从更大的数据驱动候选池中抽取 4 条，覆盖成员贡献、完成率、出勤覆盖、动作样本和训练量峰值等情况。

### 验证
- `training-partner-app`: `npm run typecheck -- --pretty false` 通过。
- `training-partner-app`: `npm test -- --runInBand` 通过，17 个套件 / 94 个用例。
- `training-partner-app`: `npm run lint` 通过，仅保留 `app/(tabs)/today.tsx` 的既有 hook dependency warning。
- 本次不涉及后端或服务器 API，无需服务器部署。

---

## v2.3.6 — 2026-07-06

**移动端记录与分析体验修复**

### 新增
- 个人训练分析新增动作筛选，可从当前范围真实训练过的动作中筛选 1RM 趋势。
- 小组动作对比新增动作筛选 Sheet，不再只展示预设动作；成员分析右上角可切换成员。

### 修复
- 移除训练休息结束后需要手动关闭的常驻提示，休息结束只更新成员就绪状态。
- 重量/次数记录器切换组时同步刷新输入草稿，并继续按成员设置的杠铃 / 哑铃加重单位步进。
- 记录页日期筛选去掉重复的“自定义”快捷入口，日期行作为唯一自定义日期筛选入口。
- 记录页右上角补录按钮改为紧凑图标按钮。
- 折线图点位支持点击显示数据；稀疏训练日期会跳过空点连接有效训练点，避免非训练日把趋势线拉断或拉到 0。
- 小组分析移除重复“成员分析”入口；成员贡献和小组趋势切换训练量 / 完成率 / 活跃度时，排行指标、进度条和图表同步变化。
- 小组历史分析、出勤、动作对比和成员分析优先读取成员 profile 头像缓存；账号头像同步支持未绑定账号 ID 的当前训练成员 fallback。

### 验证
- `training-partner-app`：`npm run typecheck`、`npm run lint` 通过（仅保留 `today.tsx` 既有 hook 依赖 warning）。
- `training-partner-app`：`npm test -- --runInBand src/tests/weight.test.ts src/tests/history.test.ts src/tests/chart-scale.test.ts` 通过。

---

## v2.3.5 — 2026-07-06

**后台管理系统排查与修复**

### 新增
- 后端新增系统计划和系统动作目录，`db:seed` 会写入 `training_plans` 与 `exercises`，后台计划管理和动作库增加兜底数据。
- 新增目录接口：`GET /api/catalog/system-plans`、`GET /api/catalog/system-exercises`。
- 新增在线同练房间 API，支持小组房间创建、查询、加入、事件上报、离开和结束。

### 修复
- 修复会员下发来源校验不一致导致“无提示失败/未成功”的问题，并支持用用户 ID、手机号、邮箱、练刻 ID 或唯一昵称发放会员。
- 修复数据修正只写日志、不更新业务表的问题；现在会真实写库、记录 before/after，并支持回滚。
- 修复 `/uploads/avatars/` 目录访问返回 `Route not found` 的问题，目录地址会返回明确说明。
- 后台会员页和数据修正页补充用户标识、成员关系修正字段提示。

### 文档
- 新增 `management-console/后台管理问题排查与修复说明.md`，逐条说明 8 个后台问题的原因、修复内容和验证方式。

### 验证
- `apps/liftmark-api`：`npm run typecheck`、`npm run build` 通过。
- `backend`：`npm run build` 通过。

---

## v2.3.4 — 2026-07-06

**默认本机成员修复**

### 修复
- 修复云同步后 `练刻3716` 这类账号成员被排到默认本机成员位置的问题。
- 训练、历史、体测、资料等个人口径统一优先使用本地训练身份，例如原来的 `zhw`。
- 当前账号云端成员同步到本地时优先绑定已有本地训练成员，并保留本地训练昵称。
- 无训练记录、无体测记录的重复账号成员会软删除，避免成员列表出现多余默认人。

---

## v2.3.3 — 2026-07-06

**账号数据恢复 + 云同步防串号修复**

### 修复
- **账号级同步游标**：`last_pull_at` 改为 `last_pull_at:{userId}`，避免切换账号后复用其他账号的拉取游标。
- **失败不推进游标**：任一云端记录应用失败时不更新拉取游标，避免“本地没恢复但下次认为已经同步过”。
- **云端恢复旧误归属数据**：`/sync/pull` 现在可按 `remote_id` 或本地 `id` 命中旧记录；若当前账号云端记录对应的本地数据被旧版本错误写到其他账号，会重新认领回当前账号。
- **归属修复收窄**：登录后的归属修复只处理小组、成员、成员资料，不再批量改训练、计划、体测等业务数据。
- **登录恢复顺序**：fullPull 先拉小组/成员结构，再拉训练/计划/体测数据，最后推送本地队列。
- **启动上传防护**：移除 App 启动时自动上传本地小组结构，防止账号切换后把旧本地数据绑定给新账号。
- **Push 状态回写**：服务端确认 push 后同步回写本地业务表的 `remote_id`、`sync_status` 和 `last_synced_at`。
- **后端防护**：`/sync/push` 不再接受跨账号 `serverId` 复用，服务端只更新当前用户已有记录。

### 验证
- 前端：`npm test -- --runInBand`（16 个套件 / 92 个用例）、`npm run typecheck -- --pretty false`、`npm run lint` 通过；lint 仅保留 `LegalDraftPage.tsx` 既有 warning。
- 后端：`npm run typecheck`、`npm run build` 通过。

---

## v2.3.2 — 2026-07-04

**本地数据库 schema 兼容修复 + 头像上传全链路修复**

### 修复
- **SQLite schema 兼容性**：新增 `ensureLocalSchemaCompatibility()` 函数，对 8 张核心表做 PRAGMA 检查 + ALTER TABLE 补列，解决旧数据库升级后 `no such column: avatar_url` 报错
- **头像上传兼容性**：React Native 0.85 的 FormData 不支持 `{ uri, name, type }` 对象格式，改用 XMLHttpRequest + Blob 方式上传，修复 `Unsupported FormDataPart implementation` 错误
- **启动顺序修复**：`_layout.tsx` 改为 await 数据库初始化完成后再加载页面，避免页面查询时迁移未执行
- **uploads 目录权限**：`.gitignore` 新增 `uploads/` 忽略规则

### 新增功能
- **同步诊断页数据库结构检查**：显示 group_members、member_profiles、account_profile_cache 等表的头像字段是否存在
- **一键修复数据库结构按钮**：缺失列时可点击修复，无需卸载重装
- **已应用迁移版本显示**：同步诊断页展示本地数据库已执行的迁移版本号

### 新增文件
- `training-partner-app/src/data/local/schemaRepair.ts` — 可复用的 schema 兼容修复函数
- `training-partner-app/src/services/syncDiagnosticsService.ts` — 同步诊断服务

### 迁移
- **Version 13**: `local_schema_repair` — 调用 `ensureLocalSchemaCompatibility()` 做全面 schema 修复

---

## v2.3.0 — 2026-07-02

**小组成员系统重构 + 邀请码 + 训练数据同步**

### 新增功能
- **邀请码系统**：创建小组后可生成邀请码，分享给朋友加入
- **待确认数据**：训练结束后可选择将数据上传给组员，组员登录后确认接受
- **头像文件上传**：头像改为文件上传方式，节省数据库空间
- **成员头像同步**：训练页面自动从服务器同步最新头像

### 服务器端
- 新增 `member_profiles` 表（成员训练资料）
- 新增 `pending_training_data` 表（待确认训练数据）
- 新增 `group_invitations` 表（邀请码）
- 新增邀请码接口：创建、查询、禁用、使用
- 新增待确认数据接口：上传、查询、确认、拒绝
- 新增头像文件上传接口：`POST /auth/avatar/upload`
- Nginx 配置更新：支持静态文件服务

### 手机端
- 新增邀请码管理页面
- 新增加入小组页面（输入邀请码）
- 新增待确认数据通知页面
- 新增训练结束选择上传组员页面
- 头像上传改为 FormData 文件上传方式

---

## v1.2.0 — 2026-06-22

**训练执行页全面重构 + 多处 UI 优化**

### 训练中页面
- 顶部导航栏改为浅色背景，左侧返回箭头 + 标题"训练中" + 副标题"增力阶段 · 第 3 周 · Day 2"，右侧红色"结束训练"按钮
- 整体从深色主题改为浅色主题，卡片白底、文字深色
- 布局重排：统计条 → 动作 Hero → 成员轮换 → 当前记录 → 休息卡 → 已完成组 → 轮换顺序 → 进度条
- 移除底部重复的"完成本组"按钮，记录卡内已有
- 新增"上一个动作"按钮，支持前后切换动作
- 移除休息卡片中多余的"跳过休息"按钮，只保留记录卡内的动态按钮
- 成员轮换：点击头像可切换当前记录人
- 训练进度条：支持点击任意动作节点跳转

### 记录卡片
- 重量/次数输入与 RPE/RIR 选择器左右分栏显示
- "完成本组"和"跳过休息"按钮并排，完成本组更醒目
- 已完成组支持内联编辑（直接修改重量/次数/RPE/RIR）和删除

### 我的页面
- Banner 背景色从深色改为浅色
- Logo 切换为浅色背景适配版本
- 图标背景色对比度提升

### 记录页面
- 新增"训练分析"快捷入口
- 修复分析页面标题显示问题

### 新增组件
- `ExerciseHeroCard` — 动作 Hero 卡片
- `GroupMemberStrip` — 成员轮换条
- `CurrentSetRecorder` — 当前记录卡片
- `CompletedSetList` — 已完成组列表
- `RotationOrderCard` — 轮换顺序卡片
- `WorkoutProgressStrip` — 训练进度条（支持 card/dock 两种模式）
- `WorkoutStatsBar` — 训练统计条

---

## v1.1.0 — 2026-06-17 ~ 2026-06-22

**UI 全面重新设计 + 功能修复**

- 4Tab 布局重构（首页/计划/记录/我的）
- 主题系统全面优化（颜色、圆角、阴影）
- 新增 MiniLineChart 迷你折线图组件
- 首页仪表盘重设计
- 应用 taste-skill / impeccable 设计规范

---

## v1.0.0 — 2026-06-14 ~ 2026-06-15

**项目初始化 + 核心功能搭建**

### v1.0.5
- 优化历史分析和手动记录功能
- 更新品牌图标和启动画面资源
- 重构手动记录体验
- 新增训练进度分析模块

### v1.0.4
- 全新历史分析界面
- 重构历史列表页
- 新增分析引擎

### v1.0.3
- 优化训练计划创建流程
- 改进运动选择器
- 新增成员头像组件
- 扩展运动分类体系

### v1.0.2
- 大幅扩展运动库和计划管理功能
- 新增运动选择器、模态弹窗组件
- 重构计划页、探索页、设置页

### v1.0.1
- 新增 Android 原生构建配置
- 优化设置页和历史记录页
- 新增成员单位设置、成员管理页
- 新增经典 PPL 训练计划模板

### v1.0.0-alpha
- 项目初始化，包含完整训练伙伴应用源码
- 技术栈：React Native + Expo + SQLite
- 功能模块：训练记录、计划管理、历史分析、运动库、成员管理、设置
## v2.7.0 - 2026-07-14

- 新建和编辑计划统一为每个动作独立的组次、强度、重量/%1RM、休息和备注处方。
- 保存前校验动作参数；训练快照优先保留固定重量处方，历史训练不受后续计划编辑影响。
- 本轮未实现动作图标、`icon_key` 映射或第三方媒体；不含数据库迁移、API 变更和服务端部署。
